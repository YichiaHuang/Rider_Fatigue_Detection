# 外送騎手疲勞風險偵測：一天黑客松執行計畫

更新：2026-09-18。以 Stage A＋MQTT＋自訂網頁即時影像完成展示閉環；Stage B 僅作選配離線比較，不部署，LLM 移至未來方向。

本版整合 `plan_stage_a_pivot.md` 的方向及一天時程、即時影像需求；如與該歷史 pivot 文件的兩天安排或優先序不同，以本版為準。

## 一、問題陳述

> 以下保留原始背景草稿；其中統計、法規日期、比較與引述尚未在本次更新中查證，使用於簡報前需另行核對來源。

疲勞駕駛是沒有酒測器的酒駕。

立法院法制局的研究指出，睡眠不足 5 小時的駕駛，反應時間與血液中含有 0.1% 酒精的駕駛沒有差別。台灣的法定酒駕上限是 0.05%——換句話說，疲勞駕駛的反應遲鈍程度是酒駕標準的兩倍。

但兩者的待遇天差地別：酒駕有酒測器、有罰則、有社會共識；疲勞駕駛什麼都沒有。台灣每年因疲勞駕駛發生的事故，約占車禍總事故的 20%。

外送員是這個問題最集中的族群，而且原因是結構性的。南台灣外送產業工會指出，訂單單價從每 2 公里 40 元降到 4 公里 40 元，迫使外送員必須完成更多訂單才能維持收入——原本 8 小時的薪酬，變成要工作 12 小時。台灣勞工陣線的問卷調查（128 份樣本）顯示，外送員單日工時 12 到 16 小時的佔 30%，超過 20 小時的也有 3%。這不是個人自制力的問題，是計價制度逼出來的。

而現在，平台有了法律壓力：

- 《外送員權益保障及外送平臺管理法》2026 年 1 月 6 日三讀通過，2026 年 7 月 21 日正式施行，是台灣首部中央層級的外送產業專法，內容包含商業保險保障與職業安全衛生教育訓練的規範。
- 高雄市的自治條例更直接：若外送員當季交通事故較前一季上升，平台業者應訂定降低事故的計畫，未依規定辦理將受罰。

平台被要求「降低事故」，但市面上沒有可用的工具。值得注意的是，立法院那份研究報告在建議事項中，明確寫了「鼓勵加裝駕駛疲勞警示之自動偵測系統」。政策方向已經指出來了，缺的是能落地的產品。

## 二、解決方案與一天版本範圍

在手機架附近安裝攝影機與 i.MX93，於裝置端抽取眼睛、嘴巴與頭部姿態特徵，以 Stage A 規則式累積評分。平台接收分數，在示範派單系統中切換「正常派單／暫停新單／恢復派單」。本次不串接真實外送平台 API。

**必要交付：真實攝影機 → Stage A → MQTT → 自訂網頁儀表板 → 暫停／恢復派單示範，並在展示模式顯示即時攝影機畫面。**

| 優先序 | 範圍 | 完成定義 |
|---|---|---|
| P0 | 即時感知、時間一致的 Stage A、MQTT、熔斷、5 騎手網頁、MJPEG 展示影像 | 1 位真實騎手＋4 位明確標示的模擬騎手；完成全流程彩排 |
| P1 | 校準錄製、Stage A 基本量化驗證、IMU 實機整合 | 核心閉環穩定後進行；IMU 不阻塞視覺版展示 |
| P2 | Stage A vs Stage B 離線比較圖、外觀美化、額外情境影片 | 有剩餘時間才做，不列為必要驗收 |
| 未來 | Stage B 訓練後量化／NPU 部署、LLM、音訊、PPG、公開資料集研究 | 不排入一天開發主線 |

設計原則：

1. **介入點是派單。** 本次展示暫停新增任務的流程，不以騎乘中突然發出警示音作為主功能。
2. **一般模式只傳分數，展示模式明確開啟影像。** 展示期間影像會離開板子、傳到展示筆電；不再使用「影像永不離開裝置」的絕對說法。
3. **分數是原型風險指標。** 門檻需校準，閉眼、哈欠、低頭的動作演示不等於已證明真實疲勞辨識或事故預防效果。

## 三、系統架構與部署位置

```text
i.MX93
  攝影機 → 單一影格擷取來源 → 最新影格緩衝區
                              ├─ DMS 模型推論（目標使用 NPU，需實測確認）
                              │    ↓ EAR / MAR / 頭部角度
                              │  CPU：PERCLOS + Stage A 規則評分
                              │    ↓ MQTT {timestamp, score}
                              │
                              └─ 展示模式：縮圖／JPEG 編碼 → MJPEG 串流

展示筆電（與板子同區網或透過 Tailscale）
  Mosquitto broker → 平台訂閱器 → 暫停／恢復派單狀態
  網頁後端 → 自訂網頁：即時畫面 + 分數曲線 + 狀態 + 4 位模擬騎手
  筆電瀏覽器 → 會場投影機／大螢幕
  校準資料離線分析（有餘裕再產出比較圖）
```

- **板子**：攝影機擷取、既有 DMS、特徵處理、Stage A、MQTT 發送，以及展示影像串流。感知推論優先於 JPEG 串流。
- **筆電**：broker、網頁後端、儀表板、模擬騎手與資料分析。若沿用 Streamlit，依賴安裝在筆電；亦可採簡單 HTML/JavaScript 網頁，依既有設計整合。
- **NPU／CPU 分工**：NPU 加速既有視覺模型；Stage A 的時間累積與狀態邏輯跑 CPU。本次沒有 Stage B NPU 部署任務。
- **既有程式位置**：`/home/fatigue-detection`；DMS bridge 引用 `/root/guardian_helmet/dms`。本文件描述的新增功能均需另行實作驗收。

## 四、軟體實作方案

### 4.1 Layer A／B：感知與特徵

沿用既有 BlazeFace／Face Mesh／Iris 推論及 `guardian_helmet_bridge.py`，輸出 EAR、MAR、head pitch，再計算 PERCLOS。先確認實際攝影機裝置、解析度、方向與鏡頭位置，不假設 `/dev/video0` 一定可直接使用既定格式。

開工前兩小時內，驗證實際安裝角度的人臉偵測、模型與 delegate 相容性、NPU 使用證據及實測推論 FPS。現有 bridge 預設 delegate 為空，僅執行現有錄影腳本不能證明 NPU 已啟用。若 NPU 整合卡住，先保留可運作的 CPU 路徑並標示實際模式，再依剩餘時間決定是否繼續排查。

### 4.2 Stage A：唯一線上評分路徑

- 沿用 PERCLOS、哈欠事件、持續低頭、分數衰減與上限的規則架構；哈欠保留 cooldown。
- **優先修正取樣率依賴**：目前 PERCLOS 異常每次 `update()` 固定加分，會隨 FPS 提高而加速累積。改為按實際時間累積持續訊號，或以固定評分節奏執行；事件加分與持續訊號分開定義。用同一段訊號在不同 FPS 下驗證近似的分數與觸發時間。
- PERCLOS 需考慮不等間距影格、有效觀測時間、啟動暖機與失去人臉；缺失資料不可當成睜眼，也不可把長時間缺口整段當作閉眼積分。
- 現有 MAR `0.3`、視覺低頭 `13°`、EAR `0.21` 為實作起始值，不代表已完成受試者校準。特徵抽取腳本從共用設定讀取門檻，避免仍使用舊值 `0.6`／`20°`。
- 頭部規則有 IMU 時可要求視覺與 IMU 一致；無 IMU 時以目前 `0.6` 降權策略作起點並明確標示。PERCLOS 與哈欠仍是視覺規則，不能宣稱所有加分都需兩種感測器同意。
- 若保留音訊程式，音訊只加成已觸發規則；本次不啟用為必要展示功能。

### 4.3 IMU 與硬體限制

MPU6050 如需量測頭部姿態，須固定於安全帽／頭部並校準零點與方向。手機架上的 IMU 只能反映支架／車體運動，不可視為頭部低垂訊號。

視覺與頭部 IMU 同時偵測低頭，仍不能直接分辨看導航與打瞌睡；需保留誤報案例，結合持續時間及其他視覺訊號評估。H2 在核心軟體整合期間確認接線與資料；未就緒時採明確標示的視覺模式。

### 4.4 MQTT、熔斷與自訂網頁

- Topic：`riders/{id}/fatigue_score`；一般模式 payload 為 `{timestamp, score}`，不含影像或位置。此 payload **含時間戳**，不可再宣稱沒有時間資訊。
- 分數以固定、可設定的節奏持續上報，例如 1 Hz，支援曲線、恢復判斷與斷訊偵測；不是只在超標時上報。
- 平台採不同 pause／resume 門檻避免來回切換；目前 `15`／`8` 為起始值，須在評分時間尺度修正後重新校準。
- 長時間收不到資料、攝影機故障或人臉無法辨識時，UI 優先顯示「未知／訊號中斷」，不能以舊的綠色狀態表示持續正常。無效感知時停止發布正常有效分數，搭配明確的健康狀態與超時處理。
- 網頁呈現 1 位真實騎手＋4 位模擬騎手的分數、趨勢、暫停／恢復狀態；真實與模擬資料來源需清楚標示。
- 展示用 EAR、MAR、PERCLOS、頭部角度、規則原因及感知狀態可透過獨立的展示狀態介面更新。關閉展示模式時，同時停用細節輸出，維持一般模式只傳分數的契約。

### 4.5 即時影像投影到網頁

**一天版本採 MJPEG。** 板子提供 JPEG 連續影格串流，網頁以 `<img>` 顯示；不經 MQTT 傳影像。網頁後端可代理串流，讓瀏覽器使用同來源路徑。

1. 攝影機只由單一程式開啟，推論、特徵錄製與 JPEG 編碼共用來源；不要讓不同程式同時搶攝影機。
2. 以 **640×480、5–10 FPS** 為展示串流起始目標，依實測資源與延遲調整；這不是目前已達成的效能。畫面 FPS 與推論 FPS 分開量測。
3. 使用有界緩衝區、只保留最新影格；瀏覽器或網路慢時丟棄過期畫面，避免回壓阻塞推論。沒有觀看者或一般模式時停止不必要的 JPEG 編碼。
4. 顯示畫面更新／連線狀態；若特徵不是來自當前顯示影格，標示更新時間，避免把非同步資訊誤當逐幀對齊。
5. 展示優先使用同一區網或既有 Tailscale 連線。瀏覽器必須實際可達影像服務；公開網站不能假設外部觀眾可直接連到 `100.71.95.53`。
6. 若後續部署 HTTPS 公開網站，影像也需經 HTTPS 並限制存取，避免瀏覽器阻擋混合內容。公開網域、對外串流與 WebRTC 不列入一天必要交付。

**兩種模式：**

| 模式 | 影像與細節 | 對外分數 | 驗收方式 |
|---|---|---|---|
| 一般模式（啟動預設） | 影像留在板子供推論，串流與展示細節介面關閉 | 持續傳 `{timestamp, score}` | 直接請求影像端點也拿不到畫面，而非只在 UI 隱藏 |
| 展示模式（操作員手動開啟） | 影像傳到展示筆電／瀏覽器，顯示「展示串流中」；不自動儲存影像 | 持續傳分數，展示介面可提供特徵與原因 | 網頁有畫面；關閉後既有串流中止，新的請求無法繼續取圖 |

「不自動儲存」是系統行為，不代表能阻止觀看者截圖。示範備援影片由團隊另外明確錄製並標為回放；與預設不錄影的即時串流分開。

### 4.6 校準與量化驗證

核心閉環完成後，修正 `record_calibration_session.py`：接真實 frame source、改為逐有效影格記錄、回報有效 FPS／人臉偵測率／缺失數；加入短段落測試選項。CSV 保留 `person_id`、`session_id`，並增加能辨認段落邊界的 `segment_id`。

目標為 **5 人 × 4 狀態 × 2 段 × 30 秒**，純動作資料 20 分鐘，含引導與換人預留約 45–60 分鐘。狀態為正常、閉眼、哈欠、低頭。第一個人完成就檢查 CSV、特徵尺度、時間戳與段落，不等所有人錄完。

- 獨立動作段落的 PERCLOS 與評分器在段落開始時重置，防止上一段歷史污染；重置表示空歷史，首個閉眼樣本不一定輸出 0。視窗不可跨越段落、受試者、session 或大段缺失資料。
- 另以連續序列驗證實際暫停、恢復與時間延遲；這組測試保留連續狀態，不能用每段重置結果代替。
- 按人分開校準與最終評估；若做留一人驗證，每折的門檻選擇只能使用其餘受試者，不能用 held-out 成績選最佳門檻後再報同一成績。
- 優先產出 Stage A 每人誤報／漏報、分母、首次觸發延遲、恢復時間、缺失率與失敗案例。未觸發的案例標示未觸發，不從延遲統計悄悄刪除。
- 第一個人的資料只能檢查流程，不能完成留一人模型驗證。資料不足時如實標示，合成資料只證明腳本能跑。

**Stage B 僅為 P2 離線比較。** 有餘裕再抽出 numpy baseline，產出同一批視窗上的 ROC／PR、時間曲線與結果表；先修正 Stage A 時間尺度，再比較視窗證據強度。Stage A 分數不是機率，不能把 Stage B 機率乘 100 就套用 Stage A 的派單門檻。相同輸入不代表兩種方法必然表現一樣，取捨理由是本次時間與驗證資料有限。這裡的「量化驗證」指計算評估指標，不是模型 int8 量化。

## 五、Demo 流程與驗收

1. 筆電開啟設計好的網頁並投影；操作員開啟展示模式，確認真實攝影機畫面及 4 位模擬騎手標示。
2. 隊員在固定展示裝置前維持正常姿勢，畫面、特徵與分數持續更新。
3. 依校準過的腳本展示閉眼、哈欠或持續低頭，解釋哪個規則產生加分；不承諾單次哈欠必定暫停派單。
4. 分數達 pause 門檻，網頁顯示暫停新增任務；恢復正常並滿足 resume 條件後恢復。若為了展示採較短觀測窗，明確標示展示設定。
5. 遮擋鏡頭或中斷資料，確認顯示未知／中斷；恢復後可重新接收資料。
6. 關閉展示模式，確認畫面與細節串流停止，但一般模式仍傳分數。

**完成標準**：以實際投影筆電連續跑至少 10 分鐘，無崩潰及明顯影格堆積，記錄串流／推論 FPS、資源用量與可觀察的延遲；重複完成至少兩次暫停／恢復流程；驗證斷訊、重新連線、重新啟動及模式切換。影像串流不能讓評分與 MQTT 停止更新。

備援優先使用彩排錄影或已有素材，畫面清楚標示「回放」。額外實際騎乘影片僅在進度允許時製作，不阻塞彩排，也不能取代即時鏈路驗收。

## 六、採用動機與產品定位

- 平台端展示一個可觀察、可解釋的風險介入流程，未來再討論真正的派單 API、休息安排與申訴機制。
- 一般模式減少傳送資料種類；展示影像是明確開啟的例外功能。
- 本次成果是受控環境原型。合規、保險效益、事故降低效果與騎手接受度需另有資料或訪談支持。

## 七、既有基礎與尚未驗證項目

2026-09-18 透過 SSH 檢查 `/home/fatigue-detection` 的結果：

| 項目 | 已觀察結果 | 不能據此推論的事 |
|---|---|---|
| 既有測試 | Stage A 腳本執行完成；pipeline 與 numpy Stage B 合成資料測試通過 | 不等於攝影機、NPU、MQTT 與網頁已整合驗收 |
| 真實錄影 | `yawn_eye_test.avi` 抽樣 67 幀，66 幀偵測到臉，約 99% | 是單一影片的人臉偵測率，不是疲勞辨識準確率；該驗證預設走 CPU |
| 錄製工具 | 真實模式尚未接上，會退出；原本每秒一列 | 仍需完成逐影格錄製與現場測試 |
| 執行環境 | `python3` 3.13.9；目前環境查無 Streamlit、matplotlib | 不能依舊文件假設分析與儀表板依賴已安裝 |
| 板子資源 | 約 1.9 GiB RAM，根目錄剩約 1 GB；有 Mosquitto 與 `/dev/ethosu0` | 裝置節點存在不代表模型已由 NPU 執行 |

工程重點是把既有感知接上可用的時間評分、網頁影像及派單示範，並記錄實際驗證結果。

## 八、黑客松後的方向

- 擴大受試者、光線、眼鏡／安全帽、姿態與騎乘情境資料，驗證誤報、漏報及泛化能力。
- Stage B 資料驅動分類器訓練、模型量化、Vela 與 NPU 部署；先證明效益再安排。
- 音訊噪音處理、PPG 生理訊號、本地 LLM 趨勢建議。
- 真正的外送平台整合、資料保留／權限設計、公開 HTTPS 服務，及需要時再評估 WebRTC。

## 九、預期問答

**「網頁看得到臉，為什麼說有隱私設計？」**
展示模式由操作員手動開啟，影像會傳到展示筆電，系統不自動錄影；一般模式關閉影像端點，只傳分數與時間戳。兩種模式都要實測，不能宣稱影像永不離開裝置。

**「頭低下來可能只是在看導航？」**
確實可能。頭部 IMU 只能協助確認姿態，無法單獨識別低頭原因；本次保留這類誤報測試，以持續時間與其他訊號調整規則。

**「NPU 在做什麼？為什麼評分不用新訓練模型？」**
感知層沿用既有深度學習模型，目標在 NPU 上執行並提供實測證據。疲勞評分使用 CPU 時序規則，方便在一天內校準、解釋與驗收。Stage B 僅在有餘裕時離線比較。

**「99% 是疲勞辨識準確率嗎？」**
不是，是單一既有影片的抽樣人臉偵測率。疲勞相關動作的誤報與漏報要另外計算，真實疲勞與道路效果尚待驗證。

**「資料會不會被拿去做績效考核？」**
一般模式限制傳送內容，但分數與時間仍可能被再利用；這需要平台政策、權限和保留規則，不能以只傳分數就宣稱架構上不可能。

**「戶外、麥克風與強制停單的效果？」**
本次只承諾受控展示環境的測試。戶外光線、噪音、導航誤判與收入影響列為後續驗證；派單狀態是模擬系統，沒有接管真實訂單。

## 十、現場準備事項

- 確認展示筆電、投影線材、區網／Tailscale、電源、攝影機與安裝角度，並讓瀏覽器實際連到影像服務。
- 指派隊員配合鏡頭測試與校準；確認安全帽 IMU 的固定方式，未完成則採視覺模式。
- 保留啟動指令、設定檔、備援素材與驗證紀錄，換人操作也能重現展示。
- 原始背景段落中的統計、法規及引述保留為待核對素材；未查到可靠來源前，不當作已核實的簡報數據。
- 有餘裕再進行外送員訪談，不影響必要展示交付。

## 十一、設備與零件

| 品項 | 用途 | 本次安排 |
|---|---|---|
| i.MX93、攝影機 | 感知、評分、影像串流 | 必要；先驗證可用性與速度 |
| 展示筆電、投影設備、網路與電源 | broker、網頁、投影、分析 | 必要；現場確認 |
| MPU6050、杜邦線、固定材料 | 頭部 IMU 整合 | 零件依原計畫已備妥，接線／安裝仍需驗證；P1 |
| MAX30102 | 心率／HRV | 依原計畫已有；留作未來，不排一天主線 |
| 麥克風 | 音訊研究 | 不排一天主線 |

## 十二、一天時程與分工

以 **10–12 小時有效工作時間**規劃；是工時估算，不是完成保證。若實際可用時間更短，先刪 P2，再縮減 P1，保留 P0 與彩排。

| 時段 | 軟體與整合 | 人員／硬體配合 | 檢查點 |
|---|---|---|---|
| 0–2 小時 | live source、NPU／CPU 實測、Stage A 時間尺度修正 | H1 確認鏡頭；H2 驗證 IMU；H3 準備筆電投影 | 決定可用的感知路徑與實際模式 |
| 2–5 小時 | MQTT、熔斷、自訂網頁、MJPEG、一般／展示模式、4 位模擬騎手 | 鏡頭前配合與網路檢查 | 第 5 小時應有完整閉環；未達則停止 P1／P2 加碼 |
| 5–7 小時 | 錄製工具、特徵門檻對齊、資料檢查 | H 組引導校準、固定設備 | 第一個人錄完立即驗證資料；IMU 可用才接入 |
| 7–9 小時 | Stage A 校準與基本指標、斷訊／失去人臉／重連處理 | 重複動作演示、核對 UI | 核心功能凍結；優先解決演示阻礙 |
| 9–12 小時 | 全流程驗收、啟動文件、備援素材、問答 | 全員彩排與投影確認 | 有餘裕才做離線比較圖，不能占用必要彩排時間 |

**團隊分工（5 人）：**

- **H1**：板子、攝影機、實際角度、NPU 執行與現場連線確認。
- **H2**：IMU 接線、頭部固定、方向校準與誤判觀察；協助失敗情境測試。
- **H3**：展示裝置、供電、投影、校準引導與彩排素材。
- **M1**：Layer B／Stage A、錄製資料、基本驗證；有餘裕再做 Stage B 離線比較。
- **M2**：MQTT、熔斷、網頁與 MJPEG 整合、模式切換、啟動流程。
- **Codex 協作**：程式修改、SSH 部署、腳本、測試、整合與文件；實體接線、鏡頭前演示和投影視覺確認由團隊執行。

## 十三、實作清單與驗證入口

以下是待執行工作，不代表本次文件更新已完成程式修改：

- [ ] 修正 `rider/stage_a_scoring.py` 的時間尺度，視離線分析需求新增 `added`；補不同 FPS、暖機、缺失與恢復測試。
- [ ] 補齊 live runner：共用攝影機來源、推論、MQTT、可設定 delegate 與健康狀態。
- [ ] 新增 MJPEG 與展示狀態介面；一般模式預設關閉，限制展示網路存取，串流不阻塞推論。
- [ ] 整合自訂網頁、模式標示、5 騎手、未知／斷線狀態與暫停／恢復。
- [ ] 修正錄製工具、段落邊界、PERCLOS 重置與共用門檻；記錄真實有效取樣率。
- [ ] 在筆電準備網頁與分析依賴；核心安裝不拉入 TensorFlow。模型量化工具僅在既有視覺模型部署確實需要時使用。
- [ ] 產出 Stage A 基本驗證與彩排紀錄；若未完成量化指標，在簡報明確標示。
- [ ] 有餘裕才建立 `scripts/lopo_baseline.py` 與 `scripts/compare_stage_a_vs_b.py`，不視為既有可執行檔。
- [ ] 隨程式實作同步更新 README 與新增當日紀錄，保留歷史 session log。

既有回歸入口（板子使用 `python3`）：

```sh
cd /home/fatigue-detection
python3 scripts/smoke_test_stage_a.py
python3 scripts/smoke_test_pipeline.py
python3 scripts/smoke_test_stage_b.py
python3 scripts/validate_against_real_footage.py /root/guardian_helmet/yawn_eye_test.avi
```

合成測試、錄影回歸、即時攝影機／MQTT／網頁驗收需分別記錄；前兩者通過不能取代第五節的完整現場驗收。